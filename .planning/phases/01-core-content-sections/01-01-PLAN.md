---
phase: 01-core-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
autonomous: true
requirements:
  - HERO-01
  - HERO-02

must_haves:
  truths:
    - "User sees a hero headline with name, role, focus, and primary stack above the fold"
    - "User sees an email CTA directly under the headline"
    - "User sees a 'View My Projects' CTA button with amber primary styling"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Hero section with headline, subline, email CTA, and primary CTA button"
      min_lines: 30
      exports: ["default"]
  key_links:
    - from: "Hero.astro"
      to: "mailto:nikkocausapin61@gmail.com"
      via: "mailto link in template"
      pattern: "mailto:nikkocausapin61@gmail.com"
    - from: "Hero.astro"
      to: "#projects"
      via: "View My Projects button anchor"
      pattern: "href=\"#projects\""
---

<objective>
Implement the Hero section as an Astro component rendering the headline, subline, email CTA, and primary CTA button per user decisions D-01 and D-02, styled to the UI-SPEC design contract tokens.
Purpose: Deliver the first user-visible section — the recruiter's initial scan point that communicates who the developer is and what they do.
Output: src/components/Hero.astro — a self-contained Astro component with all copy locked to CONTEXT.md/UI-SPEC values.
</objective>

<execution_context>
@C:/VSCODE PROJECTS/my-portfolio-2026/.opencode/get-shit-done/workflows/execute-plan.md
@C:/VSCODE PROJECTS/my-portfolio-2026/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-content-sections/01-CONTEXT.md  (D-01, D-02 — locked hero copy decisions)
@.planning/phases/01-core-content-sections/01-UI-SPEC.md  (Hero section design tokens, copywriting contract, interaction states)
@.planning/phases/01-core-content-sections/01-RESEARCH.md  (Astro component patterns, Tailwind approach)
@src/styles/global.css  (shadcn CSS variable tokens: --primary, --primary-foreground, --muted-foreground, --background, --foreground, border, ring, radius-md)
@tsconfig.json  (@/* path alias → src/*)
</context>

<tasks>

<task type="auto">
  <name>Task: Create Hero.astro component with locked copy and UI-SPEC styling</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - src/styles/global.css — verify available CSS variable token names: --primary, --primary-foreground, --background, --foreground, --muted-foreground, --border, --ring, --radius. These tokens are registered in the @theme inline block and available as Tailwind utility classes (bg-primary, text-primary-foreground, bg-background, text-foreground, text-muted-foreground, border-border, ring-ring, rounded-md).
    - .planning/phases/01-core-content-sections/01-UI-SPEC.md — Hero section design: typography (headline text-3xl font-semibold leading-tight, subline text-base, CTA button bg-primary text-primary-foreground px-6 py-3 rounded-md), color (amber primary accent reserved for CTA button), interaction states (button hover ~10% brightness reduction, focus-visible ring-2 ring-ring ring-offset-2), copywriting contract (exact strings).
    - .planning/phases/01-core-content-sections/01-CONTEXT.md — D-01 (headline structure: "Name + role + focus" with primary stack), D-02 (email CTA directly under headline), Claude's discretion (subline craft-emphasis chosen).
  </read_first>
  <action>
    Create src/components/Hero.astro with Astro frontmatter block (empty — no props needed) and a single template section.

    Semantic structure: outer container div with max-w-2xl lg:max-w-5xl mx-auto px-4 py-16 (or pt-24 pb-16 for extra hero top padding), wrapping a section element containing h1, p subline, email CTA link, and CTA button.

    Locked copy per D-01 and UI-SPEC copywriting contract:
    - h1 headline: "Nikko Causapin — Full-Stack Developer &mdash; Clean UI, Analytics-Driven Features" (rendered as HTML — &mdash; produces em dash)
    - h1 classes: text-3xl font-semibold leading-tight text-foreground
    - p subline: "I build practical web apps and internal tools that keep workflows simple and decisions clear." per Claude's discretion (craft-emphasis chosen)
    - p classes: text-base text-muted-foreground mt-4 max-w-prose
    - Email CTA link per D-02: an a tag with href="mailto:nikkocausapin61@gmail.com" displaying visible text "nikkocausapin61@gmail.com"
    - Email link classes: text-base text-primary hover:underline focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 rounded-sm

    Primary CTA button (a tag styled as button, not a button element — it's an anchor link):
    - Text: "View My Projects" per UI-SPEC copywriting contract
    - href: "#projects" (scrolls to Projects section)
    - Classes: inline-block bg-primary text-primary-foreground px-6 py-3 rounded-md text-base font-medium hover:brightness-90 focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 transition-[filter] (do NOT use scale transform on hover — UI-SPEC forbids it)
    - Placement: mt-6 (after email CTA), within a div that also contains the email link

    The email CTA and "View My Projects" button should sit in a flex container (flex flex-wrap items-center gap-4 mt-4) so they appear together below the subline per D-02 ("Email CTA appears directly under the headline").

    Container spacing: py-16 for vertical section padding per UI-SPEC spacing scale. Use pt-24 pb-16 if hero needs more breathing room above fold.

    No inline style blocks. No custom CSS. All styling via Tailwind utility classes referencing shadcn tokens.
    No props/imports needed — this is a static, self-contained component.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro exists at src/components/Hero.astro
    - File contains exact headline: "Nikko Causapin — Full-Stack Developer &mdash; Clean UI, Analytics-Driven Features"
    - File contains mailto link: href="mailto:nikkocausapin61@gmail.com"
    - File contains "View My Projects" anchor with href="#projects"
    - File contains bg-primary, text-primary-foreground classes on the CTA button
    - File contains text-3xl font-semibold leading-tight on h1
    - File contains text-muted-foreground on subline paragraph
    - npm run build exits 0 with no errors
    - No inline <style> blocks present in the file
  </acceptance_criteria>
  <done>Hero component renders headline, subline, email link, and CTA button with all locked copy. npm run build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML → Browser rendering | All content is static/hardcoded in Astro template — no user input crosses this boundary in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Hero.astro (mailto link) | accept | mailto: links are standard protocol handlers; browser handles opening. No spoofing risk from static content. |
| T-01-02 | Tampering | Hero.astro (anchor href) | accept | Internal anchor #projects only; no external untrusted URLs. No tampering surface. |
| T-01-SC | Tampering | npm dependencies | mitigate | All packages [ASSUMED] in RESEARCH.md audit; no [SLOP]/[SUS] packages. No new installs needed for this plan. |
</threat_model>

<verification>
After execution, verify:
1. `npm run build` passes with exit code 0
2. `grep -c "Nikko Causapin" src/components/Hero.astro` returns >= 1
3. `grep -c "mailto:nikkocausapin61@gmail.com" src/components/Hero.astro` returns >= 1
4. `grep -c "View My Projects" src/components/Hero.astro` returns >= 1
5. `grep -c "bg-primary" src/components/Hero.astro` returns >= 1
</verification>

<success_criteria>
- Hero.astro component exists and builds without errors
- All locked copy (D-01 headline, D-02 email CTA, UI-SPEC subline and button text) is present with exact strings
- All Tailwind classes reference valid shadcn CSS variable tokens (bg-primary, text-primary-foreground, text-muted-foreground, etc.)
</success_criteria>

<output>
Create .planning/phases/01-core-content-sections/01-01-SUMMARY.md when done
</output>
