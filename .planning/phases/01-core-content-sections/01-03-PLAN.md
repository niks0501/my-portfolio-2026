---
phase: 01-core-content-sections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AboutSection.astro
  - src/components/ServicesSection.astro
autonomous: true
requirements:
  - ABOUT-01
  - SERV-01

must_haves:
  truths:
    - "User reads a concise 2-sentence About summary describing background and working style"
    - "User sees a Services section listing exactly 4 offerings"
  artifacts:
    - path: "src/components/AboutSection.astro"
      provides: "About section with locked copy per D-07"
      min_lines: 20
      exports: ["default"]
    - path: "src/components/ServicesSection.astro"
      provides: "Services list with 4 locked items per D-08"
      min_lines: 25
      exports: ["default"]
  key_links:
    - from: "AboutSection.astro"
      to: "D-07 locked copy"
      via: "hardcoded paragraph text in template"
      pattern: "student developer focused on full-stack"
    - from: "ServicesSection.astro"
      to: "D-08 locked list"
      via: "hardcoded array or individual list items"
      pattern: "Full-stack web application development"
---

<objective>
Implement the About and Services sections as two static Astro components with locked copy per user decisions D-07 and D-08, styled to the UI-SPEC design contract.
Purpose: Deliver the recruiter's context sections — who the developer is and what work they take on.
Output: src/components/AboutSection.astro and src/components/ServicesSection.astro.
</objective>

<execution_context>
@C:/VSCODE PROJECTS/my-portfolio-2026/.opencode/get-shit-done/workflows/execute-plan.md
@C:/VSCODE PROJECTS/my-portfolio-2026/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-content-sections/01-CONTEXT.md  (D-07 locked About copy, D-08 locked Services list of 4 items)
@.planning/phases/01-core-content-sections/01-UI-SPEC.md  (About body typography text-base max-w-prose, Services single-column layout with icon+text pattern gap-2, section headings text-xl font-semibold, vertical spacing py-16, gap-8 between About and Services, container max-w-2xl lg:max-w-5xl mx-auto px-4)
@.planning/phases/01-core-content-sections/01-RESEARCH.md  (Astro static content rendering, Tailwind list utilities, no new dependencies)
@src/styles/global.css  (tokens: text-foreground, text-muted-foreground, bg-background)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutSection.astro with locked copy per D-07</name>
  <files>src/components/AboutSection.astro</files>
  <read_first>
    - .planning/phases/01-core-content-sections/01-CONTEXT.md D-07 — exact About copy: "I'm a student developer focused on full-stack web development with clean UI implementation and analytics-driven features. I build practical web apps and internal tools that keep workflows simple and decisions clear."
    - .planning/phases/01-core-content-sections/01-UI-SPEC.md — About section heading "About" with text-xl font-semibold leading-tight, body text-base max-w-prose, container max-w-2xl lg:max-w-5xl mx-auto px-4 py-16, muted text tokens for secondary content (though About body text should use text-foreground per typography mapping — body text = text-base, body uses foreground color).
  </read_first>
  <action>
    Create src/components/AboutSection.astro with empty frontmatter (no props, no imports) and a template section.

    Semantic structure:
    section element containing:
    - Container div: class="max-w-2xl lg:max-w-5xl mx-auto px-4 py-16"
    - Section heading: h2 class="text-xl font-semibold leading-tight text-foreground mb-4" — text "About"
    - Body paragraph: p class="text-base text-foreground max-w-prose" — EXACT text from D-07: "I'm a student developer focused on full-stack web development with clean UI implementation and analytics-driven features. I build practical web apps and internal tools that keep workflows simple and decisions clear."

    The copy is 2 sentences (which satisfies the 3-5 sentence REQUIREMENTS.md range — the locked decision D-07 takes precedence as the specific implementation). Do NOT add extra sentences or modify the locked text.

    The About body should use text-foreground (not text-muted-foreground) per UI-SPEC typography element-to-role mapping: body text = text-base with default foreground color.

    No inline style blocks. No animations. Static content only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - AboutSection.astro exists at src/components/AboutSection.astro
    - h2 heading text is exactly "About"
    - p body text matches D-07 exactly: "I'm a student developer focused on full-stack web development with clean UI implementation and analytics-driven features. I build practical web apps and internal tools that keep workflows simple and decisions clear."
    - p uses class text-base max-w-prose
    - h2 uses class text-xl font-semibold leading-tight
    - Container uses max-w-2xl lg:max-w-5xl mx-auto px-4 py-16
    - npm run build exits 0
    - No extra sentences beyond the 2 locked sentences
  </acceptance_criteria>
  <done>About section renders heading and locked D-07 body copy with UI-SPEC typography. npm run build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create ServicesSection.astro with locked list per D-08</name>
  <files>src/components/ServicesSection.astro</files>
  <read_first>
    - .planning/phases/01-core-content-sections/01-CONTEXT.md D-08 — exact 4 services: (1) "Full-stack web application development", (2) "Clean UI implementation", (3) "Analytics dashboards and reporting", (4) "Internal tools and workflow automation"
    - .planning/phases/01-core-content-sections/01-UI-SPEC.md — Services section heading "Services" with text-xl font-semibold leading-tight, single-column list layout on all breakpoints, each item with a marker-prefix and text, gap-2 between items, container max-w-2xl lg:max-w-5xl mx-auto px-4 py-16. Services list uses single column; gap-8 between About and Services handled by parent (index.astro) component spacing.
  </read_first>
  <action>
    Create src/components/ServicesSection.astro with empty frontmatter and a template section.

    Semantic structure:
    section element containing:
    - Container div: class="max-w-2xl lg:max-w-5xl mx-auto px-4 py-16"
    - Section heading: h2 class="text-xl font-semibold leading-tight text-foreground mb-4" — text "Services"
    - List container: div or ul class="space-y-2" (single column, vertical stacking with gap-2 between items per UI-SPEC)
    - 4 service items, each as a div or li with class="flex items-start gap-2 text-base text-foreground"

    Each item has a dash-marker prefix ("—") followed by the service text. The marker uses text-muted-foreground to visually separate from the content text. Item structure:
    ```html
    <li class="flex items-start gap-2">
      <span class="text-muted-foreground shrink-0" aria-hidden="true">&mdash;</span>
      <span>Full-stack web application development</span>
    </li>
    ```

    The 4 items in order per D-08:
    1. "Full-stack web application development"
    2. "Clean UI implementation"
    3. "Analytics dashboards and reporting"
    4. "Internal tools and workflow automation"

    Using an unordered list ul with li elements provides semantic list structure. The marker span uses aria-hidden="true" since it's decorative.

    No icon library imports needed — the em dash marker is sufficient for Phase 1 per the anti-feature list (no JS-heavy icon libraries). This keeps the site zero-JS.

    Static content only. No interactive elements.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ServicesSection.astro exists at src/components/ServicesSection.astro
    - h2 heading text is exactly "Services"
    - All 4 service items present in exact D-08 order and exact text
    - Each item uses flex items-start gap-2 with an em dash marker
    - List uses space-y-2 for gap-2 vertical spacing
    - h2 uses text-xl font-semibold leading-tight
    - Container uses max-w-2xl lg:max-w-5xl mx-auto px-4 py-16
    - npm run build exits 0
    - No icon library imports (zero JS maintained)
  </acceptance_criteria>
  <done>Services section renders heading and 4 locked D-08 service items in a single-column list with dash markers. npm run build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — none — | Both sections are static content with no links, no user input, no external resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | AboutSection.astro | accept | Static hardcoded text — no sensitive information, no dynamic content. |
| T-01-07 | Tampering | ServicesSection.astro | accept | Static hardcoded list — no user-modifiable content. |
| T-01-SC | Tampering | npm dependencies | mitigate | All packages [ASSUMED]; no new installs. |
</threat_model>

<verification>
After execution, verify:
1. `npm run build` passes with exit code 0
2. `grep -c "student developer focused on full-stack" src/components/AboutSection.astro` returns >= 1
3. `grep -c "Full-stack web application development" src/components/ServicesSection.astro` returns >= 1
4. `grep -c "Clean UI implementation" src/components/ServicesSection.astro` returns >= 1
5. `grep -c "Analytics dashboards and reporting" src/components/ServicesSection.astro` returns >= 1
6. `grep -c "Internal tools and workflow automation" src/components/ServicesSection.astro` returns >= 1
7. Verify About copy is exactly 2 sentences: `grep -c "student developer" src/components/AboutSection.astro` == 1
</verification>

<success_criteria>
- AboutSection.astro renders locked D-07 copy with correct typography
- ServicesSection.astro renders all 4 D-08 items in correct order
- Both components use UI-SPEC spacing and typography tokens
- Zero JavaScript dependencies — pure static HTML via Astro
</success_criteria>

<output>
Create .planning/phases/01-core-content-sections/01-03-SUMMARY.md when done
</output>
